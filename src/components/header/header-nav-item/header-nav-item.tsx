import React from 'react';

// @ts-ignore
// Временно поставил заглушку поскольку не знаю как тип присвоить
export default function HeaderNavItem({ children }) {
  return <div className="container pl-5 pr-5">{children}</div>
}
